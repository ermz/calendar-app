// Here i will make sure that the only way to delete an appointment is by
// having the correct email, therefore creating an admin.

// need to have to middleware in router.delete('/appointment/' [auth, admin], async (req, res))
